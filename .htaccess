<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep Laravel's application files outside the web entry path.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

<IfModule mod_negotiation.c>
    Options -MultiViews -Indexes
</IfModule>
